---
 - hosts: localhost
   gather_facts: false
   tasks:
   - name: say platform_check
     shell: echo vsphere1
     register: platform_check

   - name: platform_check debug
     debug:
       msg: "HELLO 2"
     when: platform_check.stdout in ['vsphere1','vsphere1']

